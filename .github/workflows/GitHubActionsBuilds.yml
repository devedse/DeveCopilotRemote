name: GitHubActionsBuilds

on: push

jobs:
  generate_version_number:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    outputs:
      build_number: ${{ steps.buildnumber.outputs.build_number }}
    steps:
      - name: Generate build number
        id: buildnumber
        uses: onyxmueller/build-tag-number@v1
        with:
          token: ${{secrets.github_token}}

  build_and_package:
    needs: generate_version_number
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
      - name: Install extension dependencies
        run: npm ci
      - name: Install webui dependencies
        run: cd webui && npm ci
      - name: Build webui
        run: cd webui && npm run build
      - name: Compile TypeScript
        run: npm run compile:ext
      - name: Set version
        run: |
          VERSION=1.0.${{needs.generate_version_number.outputs.build_number}}
          npm version $VERSION --no-git-tag-version
      - name: Package VSIX
        run: npx @vscode/vsce package --allow-missing-repository --skip-license --out DeveCopilotRemote.vsix
      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: DeveCopilotRemote.vsix
          path: DeveCopilotRemote.vsix

  release_github:
    permissions:
      contents: write
    needs: [generate_version_number, build_and_package]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/master'
    steps:
      - name: Download VSIX artifact
        uses: actions/download-artifact@v4
        with:
          name: DeveCopilotRemote.vsix
          path: ./artifacts
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: 1.0.${{needs.generate_version_number.outputs.build_number}}
          name: 1.0.${{needs.generate_version_number.outputs.build_number}}
          body: |
            ${{ github.event.head_commit.message }}

            ## Install
            Download the `.vsix` file and install it in VS Code:
            ```
            code --install-extension DeveCopilotRemote.vsix
            ```
          files: ./artifacts/DeveCopilotRemote.vsix
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
